name: Test env
on:
  workflow_dispatch:
jobs:
  test:
    name: Test env
    runs-on: macos-latest
    steps:
      - name: Generate .env
        run: |
          echo ${{ secrets.ENV }} > .env
          cat .env
          grep -v '^#'
          echo $(cat .env | grep -v '^#' | xargs)
